import 'package:flutter/material.dart';

class EightQuizScreen extends StatelessWidget {
  const EightQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
